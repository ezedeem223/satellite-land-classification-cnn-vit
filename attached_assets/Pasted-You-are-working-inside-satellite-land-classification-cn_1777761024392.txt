You are working inside:

satellite-land-classification-cnn-vit

This is the second academic pillar in my KAUST AI research portfolio after the ECG project.

I want you to build a focused **Satellite Research Evidence Pack**.

This must strengthen the repository as a credible research artifact without inventing metrics, without retraining, and without fake explainability outputs.

==================================================
CRITICAL SCOPE
==============

Do NOT add new model performance numbers.
Do NOT invent attention maps, Grad-CAMs, heatmaps, or saliency images.
Do NOT claim geospatial production deployment.
Do NOT claim real-world agricultural monitoring validation.
Do NOT claim state-of-the-art performance.
Do NOT download datasets unless an existing script already supports it and you are only documenting the path.
Do NOT add new external datasets.
Do NOT add a web app.
Do NOT modify core training or inference behavior.
Do NOT rewrite the whole README.

This phase is an evidence, documentation, validation, and reproducibility-strengthening pass.

==================================================
BRANCH SAFETY
=============

Before editing, verify the active repository is exactly:

satellite-land-classification-cnn-vit

Then create or switch to:

git checkout main
git pull
git checkout -b agent-lab/satellite-research-evidence-pack

If branch creation is blocked, stop and ask me to create the branch from Shell.

Work only on:

agent-lab/satellite-research-evidence-pack

Do not modify main directly.

==================================================
PASS 1 — INSPECT CURRENT REPOSITORY
===================================

Before writing anything, inspect:

* README.md
* results/metrics.json
* results/model_comparison.csv
* results/classification_report.txt if present
* results/confusion_matrix.png if present
* results/roc_curve.png or related plots if present
* configs/
* scripts/
* src/satellite_land_classification/
* tests/
* .gitattributes
* CITATION.cff
* data/README.md
* models/README.md

Confirm the current repository surface supports:

* binary agricultural vs non-agricultural satellite classification
* class folders: class_0_non_agri and class_1_agri
* Keras and PyTorch workflows
* CNN baselines
* CNN-ViT hybrid models
* preserved evaluation artifacts
* dataset not bundled
* checkpoints not bundled
* metrics are preserved historical/evaluation artifacts, not newly generated results

If any item is not verified, mark it as unverified and do not state it as fact.

==================================================
PRIMARY GOAL
============

Create:

docs/research_pack/

with these files:

1. README.md
2. PROJECT_BRIEF_KAUST.md
3. MODEL_COMPARISON_BRIEF.md
4. METRIC_PROVENANCE_MATRIX.md
5. EXPERIMENT_LIMITATION_MATRIX.md
6. CNN_VS_CNN_VIT_EXPLAINER.md
7. DATASET_AND_TASK_CARD.md
8. EXPLAINABILITY_PROTOCOL.md
9. REPRODUCIBILITY_CHECKLIST.md

Also create, if appropriate:

tools/evidence/
├── README.md
└── validate_research_pack.py

And add or update tests:

tests/test_research_pack_exists.py
tests/test_metric_provenance.py

==================================================
PART 1 — KAUST PROJECT BRIEF
============================

Create:

docs/research_pack/PROJECT_BRIEF_KAUST.md

Write a concise academic brief.

Include:

* problem definition
* why agricultural satellite classification matters
* binary task scope
* technical pipeline
* frameworks used
* CNN baseline role
* CNN-ViT hybrid role
* current evidence artifacts
* limitations
* future KAUST-style research directions

Keep wording honest.

Allowed:

* "remote-sensing classification"
* "agricultural vs non-agricultural land classification"
* "comparative CNN and CNN-ViT workflow"
* "reproducible research repository"

Forbidden:

* "operational agricultural monitoring system"
* "state-of-the-art"
* "validated for real-world deployment"
* "geospatial production platform"
* any unsupported claim about field use or policy use

==================================================
PART 2 — MODEL COMPARISON BRIEF
===============================

Create:

docs/research_pack/MODEL_COMPARISON_BRIEF.md

Use only metrics already present in:

* results/metrics.json
* results/model_comparison.csv
* README.md preserved metrics table

Do not invent new metrics.

The brief should compare:

* Keras CNN
* PyTorch CNN
* Keras CNN-ViT Hybrid
* PyTorch CNN-ViT Hybrid

Include:

* model family
* framework
* preserved accuracy
* precision
* recall
* F1
* ROC-AUC
* source artifact
* interpretation
* caution about historical/evaluation artifacts
* caution about checkpoints not bundled
* caution that preserved scores are not newly rerun in this pass

Explain carefully:

* CNNs provide local spatial feature extraction
* CNN-ViT hybrids add token/global-context modeling on CNN features
* this is not a pure standalone ViT benchmark
* the strongest preserved score belongs to PyTorch CNN-ViT Hybrid if verified from existing artifacts

==================================================
PART 3 — METRIC PROVENANCE MATRIX
=================================

Create:

docs/research_pack/METRIC_PROVENANCE_MATRIX.md

This is important.

For every metric used in the README comparison table, create a traceability row:

Columns:

* model
* family
* framework
* metric
* value
* source file
* source notebook if available
* source cell if available
* evidence confidence
* allowed wording
* forbidden wording

Evidence confidence values:

* verified from results/metrics.json
* verified from results/model_comparison.csv
* README-only
* unverified / do not state

Include explicit red lines:

* Do not call preserved metrics newly reproduced unless rerun commands were executed.
* Do not call these production scores.
* Do not imply dataset-independent generalization.
* Do not imply broader land-cover classification.

==================================================
PART 4 — EXPERIMENT LIMITATION MATRIX
=====================================

Create:

docs/research_pack/EXPERIMENT_LIMITATION_MATRIX.md

Include limitations such as:

* dataset not bundled
* checkpoints not bundled
* task is binary only
* class folders are class_0_non_agri and class_1_agri
* preserved metrics come from historical/evaluation artifacts
* some evaluation weights are external/not bundled if verified
* no fresh retraining in this evidence-pack pass
* no geospatial metadata-aware analysis yet
* no explainability artifacts generated in this pass
* CNN-ViT is hybrid, not pure ViT
* high metrics require careful provenance wording

For each limitation include:

* limitation
* why it matters
* reviewer risk
* safe wording
* future work

==================================================
PART 5 — CNN VS CNN-VIT EXPLAINER
=================================

Create:

docs/research_pack/CNN_VS_CNN_VIT_EXPLAINER.md

Explain in accessible but academic language:

* what CNN baseline learns
* why CNNs are appropriate for satellite imagery
* what the hybrid CNN-ViT design adds
* what transformer tokens represent in this project
* why the hybrid is not the same as a pure ViT
* why comparing Keras and PyTorch implementations is useful
* what the preserved results suggest
* what they do not prove

No new metrics.

No overclaims.

==================================================
PART 6 — DATASET AND TASK CARD
==============================

Create:

docs/research_pack/DATASET_AND_TASK_CARD.md

Include:

* task name
* label space
* class folders
* expected local dataset layout
* data not bundled
* expected preparation path
* binary task boundaries
* not a general land-cover taxonomy
* not geospatial metadata-aware yet
* ethical/research limitations
* reproducibility notes

==================================================
PART 7 — EXPLAINABILITY PROTOCOL
================================

Create:

docs/research_pack/EXPLAINABILITY_PROTOCOL.md

This must be a protocol only unless real supported artifacts already exist.

Include:

* what explainability could add
* Grad-CAM for CNN baselines as a future direction
* attention/token visualization for CNN-ViT hybrids as future direction
* required inputs before generating any visualization
* required model checkpoints
* required sample images
* required commands/provenance
* how to label explainability outputs
* why visualizations must not be fabricated

If no real checkpoint/sample image combination is available, state:

"No explainability maps are generated in this pass."

==================================================
PART 8 — REPRODUCIBILITY CHECKLIST
==================================

Create:

docs/research_pack/REPRODUCIBILITY_CHECKLIST.md

Include:

* install steps
* data preparation
* config files
* training commands
* evaluation commands
* prediction command
* tests
* required local assets
* expected outputs
* limitations
* what can be verified without dataset/checkpoints
* what cannot be verified without dataset/checkpoints

==================================================
PART 9 — VALIDATION TOOL
========================

Create:

tools/evidence/validate_research_pack.py

The script should validate, at minimum:

* research pack files exist
* results/metrics.json exists
* results/model_comparison.csv exists
* model names in model_comparison.csv are represented in METRIC_PROVENANCE_MATRIX.md
* forbidden phrases are absent from research pack docs:

  * state-of-the-art
  * production deployment
  * operational agricultural monitoring
  * validated real-world deployment
  * real-time monitoring platform
* required limitation phrases appear somewhere:

  * dataset is not bundled
  * checkpoints are not bundled
  * binary
  * CNN-ViT hybrid
  * preserved

The tool must not require dataset or checkpoints.

Required CLI:

python tools/evidence/validate_research_pack.py

==================================================
PART 10 — TESTS
===============

Add/update tests:

tests/test_research_pack_exists.py
tests/test_metric_provenance.py

Tests should verify:

* all required research pack files exist
* validation tool passes
* metrics.json and model_comparison.csv are parseable
* model_comparison.csv has the four expected model rows
* README metrics are traceable through results artifacts if practical
* forbidden overclaim phrases are not present in docs/research_pack/
* EXPLAINABILITY_PROTOCOL.md clearly says maps are not generated unless assets exist

Do not require dataset or checkpoints.

==================================================
PART 11 — README INTEGRATION
============================

Update README lightly only.

Add a short section near Historical Results or before Limitations:

## Research Evidence Pack

Mention:

* KAUST-oriented project brief
* model comparison brief
* metric provenance matrix
* experiment limitation matrix
* CNN vs CNN-ViT explainer
* dataset/task card
* explainability protocol

Keep this section short.
Do not rewrite the README.
Do not add new metrics.

==================================================
DO NOT BUILD
============

Do not build:

* attention maps
* Grad-CAM outputs
* heatmaps
* new training runs
* new model checkpoints
* new external dataset downloads
* portfolio website
* web app
* API server
* dashboard
* production deployment claim
* state-of-the-art claim

==================================================
REQUIRED CHECKS
===============

Run:

python -m ruff check .
python -m pytest
python tools/evidence/validate_research_pack.py

Also run:

git status --short

If existing tests fail due to unrelated historical issues, report the exact failure and still run targeted new tests if possible.

==================================================
COMMIT / PUSH
=============

If git commit is allowed:

Commit with:

Add satellite research evidence pack

Push only the feature branch:

agent-lab/satellite-research-evidence-pack

Do not merge to main.

If git operations are blocked, write the files, run checks, and provide the exact shell commands for me to commit and push.

==================================================
FINAL REPORT REQUIRED
=====================

Report only:

1. Repository confirmed
2. Active branch
3. Main not modified
4. Files created
5. Files updated
6. Evidence pack summary
7. Metric provenance summary
8. Limitation matrix summary
9. Explainability protocol status
10. README update summary
11. Tests run
12. Validation tool result
13. Commit status
14. Push status
15. Remaining limitations
